services:
  agent:
    build:
      context: ../agent
    env_file:
      - ../.env
    ports:
      - "8787:8787"
    volumes:
      - ../:/workspace:rw
    working_dir: /workspace/agent
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8787
      --reload
